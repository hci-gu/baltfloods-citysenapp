# TODOS

## Phase 1 — Various fixes

- [x] Remove wave pattern on navbar, raise up filters now that navbar takes less space
- [ ] Modal that opens when we click a map item should have much tighter layout on mobile
- [ ] Add a queryString that takes in lat/lon and overwrites current position for that session, if overwritten never try to use device location

